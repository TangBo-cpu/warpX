{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the WrapX main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
